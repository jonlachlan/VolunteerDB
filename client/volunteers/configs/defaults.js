import { LocalState } from '/client/core/context'

LocalState.setDefault('volunteers.search.results', []);
LocalState.setDefault('volunteers.search.isLoading', false);
LocalState.setDefault('volunteers.search.isError', false);
